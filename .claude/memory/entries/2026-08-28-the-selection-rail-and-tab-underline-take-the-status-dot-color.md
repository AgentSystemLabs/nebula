# The Selection Rail And The TAB UNDERLINE Take The STATUS DOT's Color Instead Of The Accent — 2026-08-28

**Asked:** "change the green underline on session rows, other focused rows, amd workspace tabs to instead match the color of the @"STATUS DOT""
→ refined: On a selected row in the focused PANEL and on the open WORKSPACE TAB, the accent-colored selection mark (green on my `forest` THEME) should take the color of that row's STATUS DOT instead of the THEME accent: the PILL ROW rail (the `█` plus its pad caps) on SESSION and WORKTREE rows, the `▌` marker on PROJECT rows, and the TAB UNDERLINE under the open WORKSPACE TAB — parent rows and tabs use their ROLLUP dot (assuming a FRESH gray dot gives a gray mark, and UNSEEN gives violet, exactly as the dot does). Rows with no STATUS DOT (PR ROW, terminal rows, overlay lists) keep the accent; the dim mark on an unfocused panel's selection stays as it is. (no questions asked)

**Did:** `crates/nebula-tui/src/ui.rs`: split `status_dot` into `status_color(status, unseen, th) -> Color` (the dot's color alone) plus the glyph, and added `selection_mark(mark, th)` — the dim→muted lift the fill already gives dim spans. `render_pill` and `render_button` grew a trailing `mark: Color`; `render_row` (overlay lists), the PR ROW under WORKTREES and the terminal / link rows under SESSIONS pass `th.accent`, agent / WORKTREE / PROJECT rows pass `status_color(rollup, unseen > 0, th)` (an archived row passes `th.dim`). `draw_workspaces_bar` carries the ROLLUP color in its per-tab tuple and paints the `▀` TAB UNDERLINE with it. Unfocused selections keep the `th.dim` rail. Tests: `pill_rail_leaves_no_untinted_quarter_at_the_corners` now passes `th.warn`; new `session_rail_takes_the_status_dot_color` (five statuses + the unfocused gray) and `tab_underline_takes_the_rollup_dot_color`; the WORKSPACES BAR test in `event_loop.rs` expects `theme.muted` under the FRESH open tab. Gate: nebula-tui lib 525 passed, clippy clean. Not visually checked — TESTBACKEND pins the colors, the glyphs did not change.

**Gotchas:**
- "Green" in this user's prompts is the `forest` THEME's **accent** (`Indexed(114)`, see `~/.nebula-dev/config.json`), not `th.ok` — a "green X" is whatever the default THEME draws cyan. Check the config's `theme` before grepping for `ok`.
- A FRESH row's dot is `th.dim`, which is also the unfocused rail color: matching the dot verbatim would make a focused FRESH selection look unfocused. `selection_mark` lifts dim to muted, the same lift `render_pill` / `render_button` give dim spans on the fill — the tests expect `th.muted` for FRESH, not `th.dim`.
- The user calls the PILL ROW's rail "the underline" — the only accent-colored per-row mark, so the reading was unambiguous, but the alias belongs in TERMS.md.
