# Skill audit — 2026-09-05, session 45c6f80a (the v0.22.0 release)

Skills invoked: release (1×), nebula-memory (1×), project-terms (1×), output-doctor (1×).
Written here because the session ran unattended; nothing below was applied.

## Proposals (pick any)

1. **TIGHTEN — release §4 "Bump the version"** (+2 lines)
   old: "One place, `Cargo.toml` under `[workspace.package]`:"
   new: "Two places: `Cargo.toml` under `[workspace.package]`, and the `nebula X.Y.Z` example in
   `docs/commands.md`. Before the release commit, `grep -rn '<old version>' --exclude-dir=target
   --exclude-dir=.claude .` — `cli.rs`'s `--base vX.Y.Z` is a tag example and stays."
   Moment: the skill's "one place" would have shipped `docs/commands.md` still saying `nebula 0.21.0`;
   an unprompted grep caught it.

2. **TIGHTEN — release §6 "Watch the build"** (+1 / −1 line)
   old: `gh run watch --exit-status $(gh run list --workflow=release.yml --limit 1 --json databaseId -q '.[0].databaseId')`
   new: `id=$(gh run list --workflow=release.yml --limit 5 --json databaseId,headBranch -q '.[] | select(.headBranch=="vX.Y.Z") | .databaseId' | head -1); gh run watch --exit-status "$id"`
   with one clause: "right after the tag push `--limit 1` can still be the previous release's run."
   Moment: the body's line contradicts the RELEASE SKILL standing gotcha the RECALL HOOK injected, so
   the poll loop was written by hand instead of copied.

3. **TIGHTEN — project-terms "Separate task"** (+2 lines)
   old: "'Separate task' means a different MEMORY LOG entry, a different session, or a different commit
   from the one that first sighted it."
   new: append "A release's roll-call of what shipped (the RELEASE NOTES, the release entry's Did) is
   the shipping task restated, not a second use: it records no sighting. A second use is a later task
   that works *on* the thing."
   Moment: six candidates (FILE TABS, NEBULA OPEN, COMMAND HELP, UPDATE INDICATOR, ATTACH REBIND,
   CLAUDE TITLE SYNC) were promoted on the strength of the v0.22.0 release entry alone; under the
   current wording every release will bulk-promote everything it lists. The opposite reading — a
   changelog is where a name becomes user-facing vocabulary — is defensible; the user decides. If
   adopted, the six rows stand (they are live features) but the rule stops the next bulk promotion.

Not proposed: output-doctor — no moment this session it would have changed; nebula-memory's
"> 5 → add TERMS until it ranks" is unsatisfiable for a bare skill trigger shared by twenty entries,
but `make recall-eval` already gates the log, so a rule change buys little.
