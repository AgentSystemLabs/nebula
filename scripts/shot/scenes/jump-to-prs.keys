# `/` over the first screen: the PALETTE lists every row nebula knows, the open pull requests last,
# each badged `draft` or `ready for review` after its title — the sidebar's states, spelled out where
# there is room — so a draft is told from a finished one before it is picked.
/
