# The same frame after switching `Focused panel tint` off and closing Settings: every cell is on the
# terminal's own background, so a transparency configured in the terminal shows through the whole window.
s
Tab
Tab
j
j
j
Enter
Escape
